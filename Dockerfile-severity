FROM mysql:5.7 ##(C)
FROM sasanlabs/owasp-vulnerableapp-facade:1.2.1 #(H)
FROM nginx:stable-alpine #(M)
FROM postgres:alpine3.20 #(I)
FROM busybox:latest #(for pagination)
FROM ubuntu:latest #(for pagination)
FROM nginx:stable-alpine #(for pagination)
FROM alpine:3.15.0 #(for pagination)
FROM rabbitmq:3.6.8 #(for pagination)
FROM debian:11 #(for pagination)
FROM checkmarx.jfrog.io/ast-docker/containers-webapp:f636860 #(for private image from Jfrog)
