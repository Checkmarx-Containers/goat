FROM library/mysql:5.7 ##(C)
FROM sasanlabs/owasp-vulnerableapp-facade:1.2.1 #(H)
FROM library/nginx:stable-alpine #(M)
FROM minio/minio:RELEASE.2024-11-07T00-52-20Z #(L)
FROM library/registry:2.8.3 #(I)
FROM manifoldai/orbyter-ml-dev:3.5 #(Malicious image with 3 malicious packages)
FROM library/busybox:latest #(for pagination)
FROM library/ubuntu:latest #(for pagination)
FROM library/nginx:stable-alpine #(for pagination)
FROM library/alpine:3.15.0 #(for pagination)
FROM library/rabbitmq:3.6.8 #(for pagination)
FROM library/debian:11 #(for pagination)